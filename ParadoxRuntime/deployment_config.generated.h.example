#pragma once

// TEMPLATE — copy to deployment_config.generated.h before building the DLL.
// Normally produced by scripts/apply-deployment-config.ps1 (not shipped publicly).
// Do NOT put credentials here; this header is compiled into the client DLL.
// Set this to the public hostname your Metagame server terminates TLS for.
#define MP_PUBLIC_HOST L"paradox.example.com"
