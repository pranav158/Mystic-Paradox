# CatalogExporter flags — copy to export_flags.txt (next to the injected process CWD).
# One KEY=VALUE per line, VALUE is 0 or 1. See ExportFlags.md for full details.
#
# Output is written to .\Items_Analysis relative to the injected process's working directory.
# Copy the produced files into the relevant service's game-data/raw/ then run the generate:* scripts.
#
# WARNING: EXPORT_SKINS_RESOLVE_STRINGTABLE is CLIENT-ONLY (calls a live engine function). Never
# enable it in a build injected into a shared dedicated server. All other flags are pure reads.

EXPORT_CATALOG=1
EXPORT_PROGRESSION=0
EXPORT_COMBAT=0
EXPORT_SKINS=0
EXPORT_SKINS_RESOLVE_STRINGTABLE=0
EXPORT_SLAYERS_PATH=0
EXPORT_WEAPON_SLOTS=0
EXPORT_HUNTS=0
EXPORT_TABLE_INVENTORY=0
EXPORT_CELLS=0
EXPORT_DROP_TABLES=0
